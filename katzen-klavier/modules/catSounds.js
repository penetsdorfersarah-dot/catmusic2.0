// Phase 2 placeholder — ElevenLabs cat sound integration
// Will replace audioEngine.js playNote/stopNote when API key is configured

export const CAT_SOUNDS_ENABLED = false;

export async function playCatSound(note) {
  // TODO Phase 2: call /api/cat-sound?note=C4 -> stream ElevenLabs audio
  console.log(`[Phase 2] Cat sound for ${note} not yet implemented`);
}
